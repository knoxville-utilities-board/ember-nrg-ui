import Component from '@glimmer/component';

export default class FreestyleHomeCardsComponent extends Component {
  iconOptions = ['', 'calendar', 'warning sign', 'settings', 'mail', 'tasks'];

  columnOptions = ['', 'one', 'two', 'three', 'four'];

  imageOptions = ['', 'https://www.unsplash.it/242/250'];
}
