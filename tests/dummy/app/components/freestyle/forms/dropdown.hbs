<FreestyleSection @name="Forms" as |Section|>
  <Section.subsection @name="Dropdown">
    <Freestyle::Usage>
      <:example>
        <NrgFormContainer as |form|>
          <form.field @label="Dropdown" as |field|>
            <field.dropdown
              @disabled={{this.dropdownDisabled}}
              @valuePath="dropdownValue"
              @model={{this}}
              @options={{this.dropdownOptions}}
            />
          </form.field>
        </NrgFormContainer>
      </:example>

      <:api as |Args|>
        <Args.Bool
          @name="disabled"
          @defaultValue={{false}}
          @value={{this.dropdownDisabled}}
          @onInput={{fn (mut this.dropdownDisabled)}}
        />
      </:api>
    </Freestyle::Usage>
  </Section.subsection>
</FreestyleSection>