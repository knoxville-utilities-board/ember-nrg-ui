<NrgFormContainer @didValidate={{didValidate}} as |form|>
  <form.field @label="Date" as |field|>
    <field.datetime @model={{this}} @valuePath="date" @value={{date}} @initializeDate={{initializeDate}} />
  </form.field>
  <form.submit-button />
</NrgFormContainer>
