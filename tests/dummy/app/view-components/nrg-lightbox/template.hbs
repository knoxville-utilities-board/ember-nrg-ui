<NrgCodeBlock @label="Lightbox Thumbnails" @codeBlockName="lightbox-thumbnails.hbs">
  <!-- BEGIN-SNIPPET lightbox-thumbnails -->
  {{#each model.photos as |photo|}}
    <NrgLightboxThumbnail @photo={{photo}} />
  {{/each}}
  <!-- END-SNIPPET  -->
</NrgCodeBlock>

<NrgCodeBlock @label="Lightbox Thumbnails with Custom Details" @codeBlockName="lightbox-thumbnails-block.hbs">
  <!-- BEGIN-SNIPPET lightbox-thumbnails-block -->
  {{#each model.photos as |photo|}}
    <NrgLightboxThumbnail @photo={{photo}}>
      <strong>{{photo.detail}}</strong>
      <span>{{counter}}</span>
    </NrgLightboxThumbnail>
  {{/each}}
  <!-- END-SNIPPET  -->
</NrgCodeBlock>

<NrgCodeBlock @label="Lightbox Videos" @codeBlockName="lightbox-videos.hbs">
  <!-- BEGIN-SNIPPET lightbox-videos -->
  {{#each model.videos as |video|}}
    <NrgLightboxVideo @video={{video}}/>
  {{/each}}
  <!-- END-SNIPPET  -->
</NrgCodeBlock>

<NrgCodeBlock @label="Lightbox Videos with Custom Details" @codeBlockName="lightbox-videos-block.hbs">
  <!-- BEGIN-SNIPPET lightbox-videos-block -->
  {{#each model.videos as |video|}}
    <NrgLightboxVideo @video={{video}}>
      <strong>{{photo.detail}}</strong>
      <span>{{counter}}</span>
    </NrgLightboxVideo>
  {{/each}}
  <!-- END-SNIPPET  -->
</NrgCodeBlock>
