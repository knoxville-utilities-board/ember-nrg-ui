'use strict';

const browsers = [
  'Chrome >= 96',
  'Firefox >= 104',
  'Safari >= 12', // Need more info on this one, get better metrics, 14 should be the latest
  'Edge >= 96',
];

module.exports = {
  browsers,
};
