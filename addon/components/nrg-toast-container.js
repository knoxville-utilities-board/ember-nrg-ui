import Component from '@ember/component';
import layout from '../templates/components/nrg-toast-container';

export default Component.extend({
  layout,
  classNames: ['top', 'right', 'ui', 'toast-container'],
});
