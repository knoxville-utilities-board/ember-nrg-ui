{{input type=type min=min max=max step=step value=_value name=name placeholder=placeholder autocomplete=(if autocomplete "on" "off") autocapitalize=autocapitalize disabled=disabled maxlength=maxLength}}
