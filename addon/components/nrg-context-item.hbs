{{#if this.renderTo}}
  {{#in-element this.renderTo insertBefore=null}}
    <a
      href=""
      role="button"
      class="{{if @disabled 'disabled'}} item {{@class}}"
      {{on "click" this.onClick}}
      ...attributes
    >
      {{yield}}
    </a>
  {{/in-element}}
{{/if}}