<div class="ui attached segment nrg-list--filter-menu {{@class}}">
  {{#if (has-block)}}
    {{yield
      (hash
        header=this
        filter=(component "nrg-list/header/filter" onChange=@onFilterChange)
        search=(component "nrg-list/header/search" onChange=@onSearchChange)
      )
    }}
  {{/if}}
  <div class="cleared"></div>
</div>