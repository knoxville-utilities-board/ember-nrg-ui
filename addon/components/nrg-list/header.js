import Component from '@ember/component';
import layout from '../../templates/components/nrg-list/header';

export default Component.extend({
  layout,
  classNames: ['ui', 'attached', 'segment', 'nrg-list--filter-menu'],
});
