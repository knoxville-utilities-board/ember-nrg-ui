import Component from '@ember/component';
import layout from '../../templates/components/nrg-master-detail/master';

export default Component.extend({
  layout,
  classNames: ['ui', 'segment', 'master-detail--master'],
});
