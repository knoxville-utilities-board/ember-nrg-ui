<video aria-label="video" role="button" preload="metadata" class="ui image photo-thumbnail" {{action openLightbox}}>
  <source src={{video.url}} alt={{video.altText}}>
</video>
{{#if hasBlock}}
  <NrgRenderTemplateBlock @templateRendered={{action setDetail}}>
    {{yield}}
  </NrgRenderTemplateBlock>
{{/if}}
