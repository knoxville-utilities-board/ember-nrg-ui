<div class="ui input {{if @fluid @fluid}} {{@class}}" type="text">
  {{#if (has-block)}}
    {{yield
      (hash
        input=(component
          "nrg-text-field/trim-input"
          type=this.type
          min=@min
          max=@max
          step=@step
          value=this.value
          name=@name
          placeholder=@placeholder
          autocomplete=@autocomplete
          autocapitalize=@autocapitalize
          disabled=@disabled
          maxLength=@maxLength
          onFocus=@onFocus
          onBlur=@onBlur
          onChange=this._onChange
        )
      )
    }}
  {{else}}
    <NrgTextField::TrimInput
      @type={{this.type}}
      @min={{@min}}
      @max={{@max}}
      @step={{@step}}
      @value={{this.value}}
      @name={{@name}}
      @placeholder={{@placeholder}}
      @autocomplete={{@autocomplete}}
      @autocapitalize={{@autocapitalize}}
      @disabled={{@disabled}}
      @maxLength={{@maxLength}}
      @onFocus={{@onFocus}}
      @onBlur={{@onBlur}}
      @onChange={{this._onChange}}
    />
  {{/if}}

</div>