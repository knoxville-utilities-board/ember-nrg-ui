<div
  hidden={{this.isHidden}}
  {{did-insert this.addToService}}
  {{will-destroy this.removeFromService}}
>
  {{#if this._renderTo}}
    {{#in-element this._renderTo}}
      {{yield}}
    {{/in-element}}
  {{else}}
    {{#unless this.isFlyout}}
      {{yield}}
    {{/unless}}
  {{/if}}
</div>