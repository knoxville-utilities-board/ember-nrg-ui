import Component from '@ember/component';
import layout from '../templates/components/nrg-icon';

export default Component.extend({
  layout,
  tagName: 'i',
  classNames: ['icon'],
  classNameBindings: ['icon'],
});
