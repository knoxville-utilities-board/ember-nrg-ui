import Component from '@ember/component';
import layout from '../templates/components/nrg-master-detail';

export default Component.extend({
  layout,

  classNames: ['ui', 'horizontal', 'segments', 'master-detail'],
});
